import OrderRow from './OrderRow';

export default class Form {

      lastname: string;
      email: string;
      street: string;
      zip: number;
      city: string;
      name: string;
      paymentMethod: string;
      totalPrice: number;
      films: OrderRow[];
}
